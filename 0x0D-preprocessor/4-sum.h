#ifndef _SUM_
#define _SUM_
/**
  *file: $-sum.h
  *Desc:Desc: Header file that defines a function-like macro SUM(x, y)
  *     that computes the sum of the numbers x and y.
  *@x: input number
  *@y: input number
  */
#define SUM(x, y) ((x) + (y))
#endif
