#include <stdio.h>
#include <string.h>
/**
  * _strlen - a function that returns the length of a string..
  * @s: length of string
  *Return: lenght of thr string
  */
int _strlen(char *s)
{
	return (strlen(s));
}
